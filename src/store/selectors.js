export const selectLoading = (state) => state.isLoading;
export const selectTodos = (state) => state.todos;
export const selectIsAdding = (state) => state.isAdding;
export const selectIsUpdating = (state) => state.isUpdating;
export const selectIsDeleting = (state) => state.isDeleting;
export const selectRefreshTodos = (state) => state.refreshTodos;
